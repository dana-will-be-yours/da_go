(()=>{
'use strict';
const $=id=>document.getElementById(id);
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let bundle=null, state=null;
function formCharacter(){const fd=new FormData($('startForm'));return {name:String(fd.get('playerName')||'旅人'),roles:fd.getAll('roles'),gender:fd.get('gender'),origin:fd.get('origin'),trait:fd.get('trait'),skills:{observe:1,speech:1,outer:1,study:1}}}
function ensureStart(){const form=$('startForm');if(!form||form.dataset.modularBound)return;form.dataset.modularBound='1';form.addEventListener('submit',ev=>{ev.preventDefault();startGame(formCharacter())});}
function startGame(character){state=window.DaGoState.initialState(bundle,character);window.DaGoRules.recalcAttrs(state);window.DaGoSave.save(state);$('startPanel').hidden=true;$('playPanel').hidden=false;render();}
function loadExisting(){const old=window.DaGoSave.load();if(old){state=old;window.DaGoRules.recalcAttrs(state);$('startPanel').hidden=true;$('playPanel').hidden=false;render();return true}return false}
function statLine(){const s=state.stats;return `精神 ${s.spirit}｜鎮定 ${s.composure}｜疑心 ${s.suspicion}｜疲勞 ${s.fatigue}｜飢餓 ${s.hunger}｜錢 ${s.coin}｜氣血 ${s.hp}/${s.hpMax}`}
function renderOverview(){const box=$('overviewBox');if(!box||!state)return;box.innerHTML=`<p>${esc(state.player?.name||'旅人')}</p><p>${esc(statLine())}</p><p>第 ${esc(state.stats.day)} 日 / ${esc(state.stats.hour)}</p><p>來源：${esc(bundle.__bundle_source||'unknown')}</p>`}
function render(){const p=window.DaGoPassage.byId(bundle,state.current_passage);if(!p)return;window.DaGoPassage.enter(state,bundle,p.id||p.passage_code);$('passageTitle').textContent=p.title||p.id;$('passageMeta').textContent=p.location||p.location_name||'';$('passageText').innerHTML='<p>'+esc(window.DaGoPassage.textOf(p)).replace(/\n\n/g,'</p><p>')+'</p>';const choices=window.DaGoPassage.choicesOf(p,state);$('choiceList').innerHTML=choices.map((c,i)=>`<button type="button" data-choice="${i}">${esc(c.text||c.choice_text||'選項')}</button>`).join('');$('choiceList').querySelectorAll('[data-choice]').forEach(btn=>btn.addEventListener('click',()=>choose(choices[Number(btn.dataset.choice)])));$('passageFooter').textContent=state.last_result?.check?.used?`檢定：${state.last_result.check.skill} ${state.last_result.check.total}/${state.last_result.check.dc} ${state.last_result.result}`:'';renderOverview();}
function choose(choice){window.DaGoPassage.applyChoice(state,bundle,choice);window.DaGoSave.save(state);render();}
function panel(name){if(name==='saves'){return `<button id="saveNow">保存</button> <button id="exportSave">下載存檔</button> <button id="restartGame">重開</button> <button id="exportLog">匯出語料 playlog</button>`}if(name==='journal')return (state?.journal||[]).map(x=>`<p>${esc(x.text)}</p>`).join('')||'<p>尚無日誌。</p>';if(name==='stats')return `<pre>${esc(JSON.stringify(state?.stats||{},null,2))}</pre>`;if(name==='social')return `<pre>${esc(JSON.stringify(state?.relationships||{},null,2))}</pre>`;return `<pre>${esc(JSON.stringify(state||{},null,2))}</pre>`}
function bindPanels(){document.querySelectorAll('[data-action="panel"]').forEach(btn=>btn.addEventListener('click',()=>{if(!state)return;const name=btn.dataset.panel;$('overlayTitle').textContent=btn.textContent;$('overlayContent').innerHTML=panel(name);$('overlayBackdrop').classList.remove('hidden');$('saveNow')?.addEventListener('click',()=>window.DaGoSave.save(state));$('exportSave')?.addEventListener('click',()=>window.DaGoSave.download(state));$('restartGame')?.addEventListener('click',()=>{window.DaGoSave.clear();location.search='?reset=1'});$('exportLog')?.addEventListener('click',()=>window.DaGoExportPlaylog.download(state));}));$('closeOverlay')?.addEventListener('click',()=>$('overlayBackdrop').classList.add('hidden'));}
async function boot(){bundle=window.DaGoState.normalizeBundle(await window.DaGoRuntimeBundlePromise);ensureStart();bindPanels();if(new URLSearchParams(location.search).has('reset'))window.DaGoSave.clear();loadExisting();}
window.DaGoModularRuntime=Object.freeze({boot});
boot();
})();
